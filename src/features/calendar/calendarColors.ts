/** Colours for calendars the server has not coloured; also the first swatches offered. */
export const FALLBACK_COLORS = ['#2d5bd1', '#0e9488', '#c2560f', '#7c3aed', '#b91c62', '#4d7c0f']

/** What the calendar dialog offers: the fallbacks plus two neutrals-ish extras. */
export const CALENDAR_SWATCHES = [...FALLBACK_COLORS, '#ca8a04', '#64748b']
